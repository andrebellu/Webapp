<mvc:View controllerName="sap.ui.demo.walkthrough.controller.Rapportini"
	xmlns:l="sap.ui.layout"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:u="sap.ui.unified" class="viewPadding">

	<App>
		<Panel>

			<Label text="Rapportini" class="titleRP"/>
			<!-- 
				<FlexBox class="fl">
					<l:VerticalLayout>
						<u:Calendar class="calendar" select="handleCalendarSelect" minDate="{path: '/minDate'}" maxDate="{path: '/maxDate'}" disabledDates="{path: '/disabled'}" showCurrentDateButton="true">
							<u:disabledDates>
								<u:DateRange startDate="{start}" endDate="{end}"/>
							</u:disabledDates>
						</u:Calendar>
					</l:VerticalLayout>
				</FlexBox>
			-->
			<Button class="btnDP" ariaHasPopup="Dialog" text="Open Date Picker" press=".openDatePicker" />

			<DatePicker id="HiddenDP" hideInput="true" change=".changeDateHandler" showCurrentDateButton="true"/>

			<List noDataText="No element to display" items="{/items}">
				<items>
					<StandardListItem type="Navigation" title="{title}" description="{subTitle}" icon="sap-icon://activities" class="item" press=".clicked"/>
				</items>
				<swipeContent>
					<Button press="handleEdit" icon="sap-icon://overflow"/>
				</swipeContent>
			</List>

		</Panel>
	</App>

</mvc:View>